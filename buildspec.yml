version: 0.2
phases:
  install:
    commands:
      - aws cloudformation package --template-file template.yml --s3-bucket aws-sam-cli-managed-default-samclisourcebucket-wi4wwgqkyzf --output-template-file packaged.yml

artifacts:
  type: zip
  files:
    - template.yml
    - packaged.yml
